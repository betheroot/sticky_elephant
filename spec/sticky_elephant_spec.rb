require "spec_helper"

describe StickyElephant do
  it "has a version number" do
    expect(StickyElephant::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
